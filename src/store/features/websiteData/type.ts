export type WebsiteDataState = {
  previewUrl: string;
  redirectUrl: string;
  promptValue: string;
  defaultUrlValue: string;
  defaultPromptValue: string;
  isPreviewLoading: boolean;
  hooryConversationId: number;
};
